name: Post-Merge Maintenance

on:
  pull_request_target:
    types: [closed]
    branches:
      - main

permissions:
  contents: write
  pull-requests: read

jobs:
  prepare:
    name: Update changelog and core release metadata
    if: ${{ github.event.pull_request.merged == true }}
    runs-on: ubuntu-24.04
    outputs:
      harness_changed: ${{ steps.maintenance.outputs.harness_changed }}
      harness_release_tag: ${{ steps.maintenance.outputs.harness_release_tag }}
      maintenance_ref: ${{ steps.maintenance.outputs.maintenance_ref }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0

      - name: Configure Git author
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Update maintenance files
        id: maintenance
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          set -euo pipefail

          git pull --ff-only origin main

          pr_json="$(gh pr view "$PR_NUMBER" --json number,title,author,mergeCommit)"
          pr_title="$(jq -r '.title' <<<"$pr_json")"
          pr_author="$(jq -r '.author.login // "unknown"' <<<"$pr_json")"
          merge_sha="$(jq -r '.mergeCommit.oid // empty' <<<"$pr_json")"
          changed_files_json="$(gh api --paginate --slurp \
            "repos/$GITHUB_REPOSITORY/pulls/$PR_NUMBER/files?per_page=100")"
          changed_files="$(jq -r '.[][] | .filename' <<<"$changed_files_json")"

          harness_changed=false
          if scripts/harness-release-changed.sh <<<"$changed_files"; then
            harness_changed=true
          fi

          harness_release_tag=""
          if [ "$harness_changed" = true ]; then
            old_version="$(awk -F'"' '/^version = / { print $2; exit }' crates/harness/Cargo.toml)"
            IFS=. read -r major minor patch <<<"$old_version"
            new_version="$major.$minor.$((patch + 1))"
            harness_release_tag="harness-v$new_version"

            NEW_VERSION="$new_version" perl -0pi -e 's/(\[package\]\s+name = "harness"\s+version = ")[^"]+(")/$1$ENV{NEW_VERSION}$2/s' crates/harness/Cargo.toml
            NEW_VERSION="$new_version" perl -0pi -e 's/(name = "harness"\nversion = ")[^"]+(")/$1$ENV{NEW_VERSION}$2/' Cargo.lock
            printf '%s\n' "$harness_release_tag" > scripts/harness-release-tag
          fi

          date_utc="$(date -u +%Y-%m-%d)"
          old_changelog="$(mktemp)"
          cp CHANGELOG.md "$old_changelog"
          entry="$(mktemp)"
          {
            printf '## %s - PR #%s\n\n' "$date_utc" "$PR_NUMBER"
            printf -- '- %s (@%s)\n' "$pr_title" "$pr_author"
            [ -z "$merge_sha" ] || printf -- '- Merge commit: `%s`\n' "$merge_sha"
            if [ "$harness_changed" = true ]; then
              printf -- '- Harness core candidate: `%s` (publication requires platform proof)\n' "$harness_release_tag"
            else
              printf -- '- Harness core release: not required\n'
            fi
            python3 scripts/render-changelog-files.py --limit 20 <<<"$changed_files_json"
            printf '\n'
          } > "$entry"
          {
            sed -n '1p' "$old_changelog"
            printf '\n'
            cat "$entry"
            sed '1d' "$old_changelog" | sed '/./,$!d'
          } > CHANGELOG.md

          if git diff --quiet; then
            maintenance_ref="$(git rev-parse HEAD)"
          else
            if [ "$harness_changed" = true ]; then
              commit_message="chore(release): prepare $harness_release_tag"
            else
              commit_message="docs(changelog): record PR #$PR_NUMBER"
            fi
            git add CHANGELOG.md Cargo.lock crates/harness/Cargo.toml scripts/harness-release-tag
            git commit -m "$commit_message"
            git push origin HEAD:main
            maintenance_ref="$(git rev-parse HEAD)"
          fi

          {
            echo "harness_changed=$harness_changed"
            echo "harness_release_tag=$harness_release_tag"
            echo "maintenance_ref=$maintenance_ref"
          } >> "$GITHUB_OUTPUT"

  release-harness:
    name: Build and publish Harness core
    needs: prepare
    if: ${{ needs.prepare.outputs.harness_changed == 'true' }}
    uses: ./.github/workflows/harness-release.yml
    permissions:
      contents: write
    with:
      release_tag: ${{ needs.prepare.outputs.harness_release_tag }}
      checkout_ref: ${{ needs.prepare.outputs.maintenance_ref }}
